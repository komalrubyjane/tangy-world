// Facade — the ONLY module UI components should import for the artist
// assignment-request workflow. Same pattern as conversationService.js.
export { assignmentRequestService as assignmentService } from './assignmentRequestService';
